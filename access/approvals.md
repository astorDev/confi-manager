# Configuration Change Approvals

In case of production scenarios configuration shouldn't be changed that easy. Normally a change of configuration will require an approval from somebody.
Since configuration is stored as JSON an approval procedure will likely look very close to Pull Requests / Merge Requests procedure.

> 🚧 Pending far more information